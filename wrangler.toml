name = "nuvio-subs-pt"
main = "src/index.js"
compatibility_date = "2025-06-01"

# Workers AI e o tradutor por defeito: nao precisa de chave nenhuma.
[ai]
binding = "AI"

# Cache das legendas ja traduzidas / descarregadas.
# Cria com:  npx wrangler kv namespace create SUBS
# e cola o id devolvido aqui.
[[kv_namespaces]]
binding = "SUBS"
id = "7c87659f495d45109af05323854f169e"

[vars]
# Idioma preferido apresentado primeiro: "pt" (Portugal) ou "pt-BR" (Brasil).
PREFERRED_PT = "pt"
# Motor de traducao: workersai | workersai-m2m | deepl | google | libre | none
TRANSLATE_PROVIDER = "workersai"
# Modelos para TRANSLATE_PROVIDER = workersai. Aceita lista separada por
# virgulas; ha sempre modelos de reserva atras do que aqui esta.
WORKERSAI_MODEL = "@cf/meta/llama-3.3-70b-instruct-fp8-fast"
# Linguas de partida aceites para traducao, por ordem de preferencia.
TRANSLATE_FROM = "en,tr"
# Numero maximo de chamadas ao tradutor por pedido (o plano Free da Cloudflare
# limita a 50 subrequests por pedido; deixa margem para as fontes).
MAX_TRANSLATE_CALLS = "40"
# Lotes traduzidos ao mesmo tempo. Com 6 um episodio levava 41s, o que passava
# da janela do prewarm; com 12 fica a cerca de metade.
TRANSLATE_CONCURRENCY = "12"
# Anime: "exclude" (nada), "no-translation" (so legendas reais) ou "include".
# Este addon e para filmes e series; anime tem addons dedicados.
ANIME_POLICY = "exclude"

# Anuncia o recurso `stream` no manifesto do addon turco (/turcas/manifest.json).
# "0" retira-o (mas a rota /stream continua a responder: e' por la que o plugin
# do NuvioTV descobre qual e' o video). Com o plugin instalado queremos "0" —
# senao aparecem duas entradas por episodio, e uma delas abre o YouTube fora da
# aplicacao em vez de tocar por dentro.
STREAMS = "0"
# Duracao minima, em minutos, para um video poder ser o episodio. Abaixo disto
# e' fragman, resumo ou excerto — que e' o que a busca devolve em primeiro.
YOUTUBE_MIN_MINUTES = "40"
# Quantos videos oferecer por episodio.
STREAM_RESULTS = "3"

# Legendas turcas do proprio video oficial no YouTube — umas escritas por
# gente, outras geradas automaticamente. Sao dois pedidos e dezenas de KB, e
# entram quando as fontes normais nao devolvem nada em lingua nenhuma.
# "0" desliga.
YOUTUBE_CAPTIONS = "1"
# Quanto tempo esperar antes de voltar a procurar, conforme o porque de nao ter
# encontrado: minutos quando o YouTube recusou (defesa anti-bot a apanhar o IP
# do datacentro), horas quando o video mesmo nao tem legendas.
CAPTIONS_RETRY_MINUTES = "15"
CAPTIONS_MISS_HOURS = "12"
CAPTIONS_CACHE_DAYS = "180"

# Legenda a partir do audio do proprio episodio (Whisper via Workers AI).
#
# DESLIGADO, e nao por estar incompleto: o codigo funciona e esta testado. O
# que nao funciona e' o acesso. Descarregar 128 MB por episodio a partir de um
# IP de datacentro aciona a defesa anti-bot do YouTube — medido: o primeiro MB
# passa, o segundo devolve `403`, e a seguir o proprio `youtubei/v1/player`
# passa a responder `LOGIN_REQUIRED` durante varios minutos. Ligar isto degrada
# tambem os streams turcos, que dependem do mesmo IP.
#
# Ver PLANO.md. O caminho que resta e' o plugin descarregar na televisao, de um
# IP residencial, e mandar os blocos ao Worker.
ASR = "0"
# Segundos de audio por bloco mandado ao Whisper. Com 480 (8 min) um episodio
# de 2h20 da 18 blocos; blocos maiores arriscam o tecto de tamanho do modelo.
ASR_BLOCK_SECONDS = "480"
# Blocos por passagem.
ASR_BLOCKS_PER_PASS = "8"
# Tecto de subpedidos por passagem. Nao e' o numero de blocos que conta: um
# bloco de 8 min sao ~7,7 MB e o googlevideo so serve 1 MB de cada vez, logo
# nove subpedidos por bloco. A Cloudflare corta aos 50 por invocacao.
ASR_SUBREQUEST_BUDGET = "30"
# Falhas seguidas antes de desistir da passagem. Sem isto, um `403` fazia
# percorrer os 18 blocos todos para nada.
ASR_MAX_FAILURES = "3"
# Lingua falada no audio.
ASR_LANGUAGE = "tr"
# Um episodio transcrito nunca muda.
ASR_CACHE_DAYS = "365"
# Modelos de transcricao, por ordem. Ha reservas atras do que aqui estiver.
WHISPER_MODEL = "@cf/openai/whisper-large-v3-turbo"

# Aquece a traducao em background assim que o Nuvio pede a lista de legendas.
PREWARM = "1"
# Dias de validade da cache KV.
CACHE_DAYS = "30"

# Segredos (nao os metas aqui — usa `npx wrangler secret put NOME`):
#   SIGNING_KEY     obrigatorio  chave para assinar os URLs /sub/*.srt
#   DEEPL_API_KEY   opcional     so se TRANSLATE_PROVIDER = deepl
#   LIBRE_URL       opcional     endpoint LibreTranslate
#   LIBRE_API_KEY   opcional
#   SUBDL_API_KEY   opcional     adiciona o SubDL como segunda fonte
#   TMDB_API_KEY    opcional     catalogos, resolucao de ids e streams turcos
#   YOUTUBE_API_KEY opcional     usa a API oficial em vez de ler a pagina de
#                                resultados; so vale a pena se a busca sem
#                                chave comecar a falhar a partir da Cloudflare
